@model List<Series>
@{
    ViewData["Title"] = "Начална страница";
}

<div class="welcome-section text-center">
    <div class="container">
        <h1 class="display-4 fw-bold mb-4">🎬 Добре дошли в TV Series App</h1>
        <p class="lead mb-4">Открийте света на вашите любими сериали, актьори и епизоди</p>
        <div class="row justify-content-center">
            <div class="col-auto">
                <a asp-controller="Series" asp-action="Index" class="btn btn-light btn-lg me-3">
                    <i class="fas fa-film me-2"></i>Разгледай сериали
                </a>
                <a asp-controller="Actors" asp-action="Index" class="btn btn-outline-light btn-lg">
                    <i class="fas fa-users me-2"></i>Виж актьорите
                </a>
            </div>
        </div>
    </div>
</div>

@if (Model != null && Model.Any())
{
    <div class="container mt-5">
        <h2 class="text-center mb-4">🎬 Топ сериали</h2>
        <div class="row">
            @foreach (var series in Model.Take(6))
            {
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card card-custom h-100">
                        <img src="@series.PosterUrl" class="card-img-top" alt="@series.Title"
                             style="height: 400px; object-fit: cover;"
                             onerror="this.src='https://image.tmdb.org/t/p/original/x04m6FF6qHpvMn24kQMpqsrDxVK.jpg'">
                      
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@series.Title</h5>
                            <p class="card-text flex-grow-1">
                                @if (series.Description.Length > 100)
                                {
                                    @series.Description.Substring(0, 100)

                                    <text>...</text>
                                }
                                else
                                {
                                    @series.Description
                                }
                            </p>
                            <div class="mt-auto">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <span class="badge bg-primary">⭐ @series.Rating/10</span>
                                    <span class="badge bg-secondary">📺 @series.Seasons сезона</span>
                                    <span class="badge bg-info">@series.Genre</span>
                                </div>
                                <a asp-controller="Series" asp-action="Details" asp-route-id="@series.Id"
                                   class="btn btn-primary-custom w-100">
                                    <i class="fas fa-info-circle me-2"></i>Детайли
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="text-center mt-4">
            <a asp-controller="Series" asp-action="Index" class="btn btn-outline-primary btn-lg">
                <i class="fas fa-list me-2"></i>Виж всички сериали
            </a>
        </div>
    </div>
}

<div class="container mt-5">
    <div class="row text-center mb-5">
        <div class="col-md-4">
            <div class="feature-icon">
                <i class="fas fa-film"></i>
            </div>
            <h4>📺 Сериали</h4>
            <p>Разгледайте нашата обширна колекция от сериали с пълна информация за всеки един.</p>
            <a asp-controller="Series" asp-action="Index" class="btn btn-primary-custom">Виж сериали</a>
        </div>
        <div class="col-md-4">
            <div class="feature-icon">
                <i class="fas fa-users"></i>
            </div>
            <h4>🎭 Актьори</h4>
            <p>Запознайте се с актьорския състав, техните биографии и постижения.</p>
            <a asp-controller="Actors" asp-action="Index" class="btn btn-primary-custom">Виж актьори</a>
        </div>
        <div class="col-md-4">
            <div class="feature-icon">
                <i class="fas fa-play-circle"></i>
            </div>
            <h4>🎬 Епизоди</h4>
            <p>Прегледайте всички епизоди, рейтинги и подробности за всеки сезон.</p>
            <a asp-controller="Episodes" asp-action="Index" class="btn btn-primary-custom">Виж епизоди</a>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-12">
            <div class="card card-custom">
                <div class="card-body text-center">
                    <h3 class="card-title">🚀 Започнете сега!</h3>
                    <p class="card-text">Регистрирайте се, за да получите пълен достъп до всички функции на платформата.</p>
                    <div class="mt-4">
                        <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary-custom me-3">
                            <i class="fas fa-user-plus me-2"></i>Регистрирай се
                        </a>
                        <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-outline-primary">
                            <i class="fas fa-sign-in-alt me-2"></i>Влез в акаунт
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>